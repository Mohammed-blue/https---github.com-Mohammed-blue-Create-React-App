import HYGIEIAFITNESS from "../../app/shared/HYGIEIAFITNESS";

export const selectAllCards = () => {
    return HYGIEIAFITNESS;
}

// export const  selectExerciseById = (id) => {
//         return HYGIEIAFITNESS.find(
//             (campsite) => campsite.id === parseInt(id)
//             );
// };
